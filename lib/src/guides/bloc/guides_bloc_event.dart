/// List of events that needs to manipulate with guides page
/// Events looks like enum because of easy converting to/from indexed list
enum GuidesEvent {
  Products,
  Elements,
  Favorite,
  Conditions,
  Analysis,
}
