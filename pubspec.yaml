name: inmyfit
description: InMyFit application

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^0.1.2
  shared_preferences: ^0.5.1+2
  flutter_local_notifications: ^0.6.0
  flutter_calendar_carousel: ^1.3.15+6
  bloc: ^0.11.1
  flutter_bloc: ^0.9.1
  gradient_app_bar: ^0.0.1
  flutter_slidable: ^0.4.9


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true